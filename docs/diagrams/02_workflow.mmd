%% ⚠ SUPERSEDED — DO NOT RENDER THIS FILE.
%% The live docs/diagrams/02_workflow.svg is HAND-AUTHORED (a three-column workflow:
%% "Your decisions" (the HITL gates) | the flow | "Tools & systems", in the style of the
%% reference Yuyan provided). Mermaid can't produce that aligned three-column look, so the
%% SVG is maintained by hand. Running mmdc on this .mmd would OVERWRITE the designed SVG —
%% don't. Edit the .svg directly instead.
%%
%% This stub is kept only so the file still parses; it is not the source of the live diagram.
graph TB
    A[See 02_workflow.svg — hand-authored, do not regenerate]

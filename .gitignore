# Secrets — never commit the real key
.env

# Python virtualenv and caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Static Pages build artifact (assembled by scripts/build_pages.py, deployed to gh-pages)
site/
